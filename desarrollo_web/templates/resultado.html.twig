<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        .header img {
            max-height: 80px;
        }
        .header h1 {
            font-size: 20px;
            text-align: right;
        }
        .content {
            margin: 20px;
        }
        .image-container img {
            max-width: 100%;
            height: auto;
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="{{ ruta_logo }}" alt="Logo">
        <h1>IAL</h1>
    </div>
    <div class="content">
        <h2>{{ titulo }}</h2>
        <p><strong>Fecha:</strong> {{ fecha }}</p>
        <p><strong>Investigador:</strong> {{ investigador }}</p>
        <div class="image-container">
            <img src="{{ ruta_resultado }}" alt="Resultado">
        </div>
    </div>
</body>
</html>
