<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        .header img {
            max-height: 80px;
        }
        .header h1 {
            font-size: 20px;
            text-align: right;
        }
        .content {
            margin: 20px;
        }
        .image-container img {
            max-width: 100%;
            height: auto;
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="{{ logoBase64 }}" alt="logo" class="logo-img">
        <h1>IAL</h1>
    </div>
    <div class="content">
        <h2>{{ test.nombreAlt }}</h2>
        <p><strong>Fecha:</strong> {{ test.fechaHora|date('d/m/Y H:i') }}</p>
        <p><strong>Investigador:</strong> {{ test.usuario.nombreCompleto }}</p>
        <div class="image-container">
            <img src="{{ imagenBase64 }}" alt="Resultado">
        </div>
    </div>
</body>
</html>
